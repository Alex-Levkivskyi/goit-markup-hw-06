/* bазові стилі для мобільного меню (від 320px) */
.menu-container {
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  background-color: #ffffff;
  z-index: 999;
  padding: 24px 24px 40px 40px;
  display: flex;
  flex-direction: column;
  justify-content: space-between;

  /* Анімація появи ,ховаємо за екран праворуч */
  transform: translateX(100%);
  transition: transform 250ms cubic-bezier(0.4, 0, 0.2, 1);
}

/* Клас, який додається через JavaScript для відкриття */
.menu-container.is-open {
  transform: translateX(0);
}

.menu-close-btn {
  display: block;
  margin-left: auto;
  margin-bottom: 24px;
  background-color: transparent;
  border: none;
  cursor: pointer;
}

.mobile-menu-list {
  list-style: none;
  padding: 0;
  margin: 0;
}

.mobile-menu-item:not(:last-child) {
  margin-bottom: 40px;
}

.mobile-menu-link {
  font-weight: 700;
  font-size: 36px;
  line-height: 1.11;
  letter-spacing: 0.02em;
  color: #2e2f42;
  text-decoration: none;
}

.mobile-menu-link.current {
  color: #404bbf;
}

.mobile-address-list {
  list-style: none;
  padding: 0;
  margin: 0;
  margin-bottom: 48px;
}

.mobile-address-item:not(:last-child) {
  margin-bottom: 24px;
}

.mobile-address-link {
  font-style: normal;
  font-weight: 500;
  font-size: 20px;
  line-height: 1.2;
  letter-spacing: 0.02em;
  color: #434455;
  text-decoration: none;
}

.mobile-address-link.tel-link {
  color: #4d5ae5;
}

.mobile-soc-list {
  display: flex;
  justify-content: flex-start;
  gap: 40px;
  list-style: none;
  padding: 0;
  margin: 0;
}

.mobile-soc-link {
  display: flex;
  justify-content: center;
  align-items: center;
  width: 40px;
  height: 40px;
  background-color: #4d5ae5;
  border-radius: 50%;
}

/* ХОВАЄМО МЕНЮ НА ПЛАНШЕТАХ ТА ДЕСКТОПАХ */
@media screen and (min-width: 768px) {
  .menu-container {
    display: none;
  }
}
